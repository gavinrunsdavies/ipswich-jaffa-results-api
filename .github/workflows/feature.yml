# 🚀 Deploy to JAFFA test website when pushing to any feature branch

name: 🚀 Deploy to test website on push

on:
  push:
    branches: [ 'feature/**' ]

  # Allows manual trigger
  workflow_dispatch:

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    environment: Test JAFFA

    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v2

      # ✅ Step 1: Create the OpenAI config file dynamically from GitHub secret
      - name: 🧩 Create OpenAI config
        run: |
          mkdir -p config
          echo "<?php define('OPEN_AI_API_SECRET', '${{ secrets.OPEN_AI_API_SECRET }}');" > config/openai.php
          echo "✅ Created config/openai.php"

      # ✅ Step 2: Deploy files to test site via FTP
      - name: 📂 Sync files to test
        uses: Wandalen/wretry.action@master
        with:
          action: SamKirkland/FTP-Deploy-Action@4.3.0
          with: |
            server: ftp.ipswichjaffa.org.uk
            username: githubftp@test.ipswichjaffa.org.uk
            password: ${{ secrets.FtpServerPassword }}
            server-dir: public_html/wp-content/plugins/ipswich-jaffa-results-api/
            dry-run: false
            exclude: |
              **/.git*
              **/.git*/**
              Tests/**
              Config.php
        attempt_limit: 10
        attempt_delay: 2000
